#include <iostream>

#define ll long long
using namespace std;

int main() {
  // (void)freopen("input.txt", "r", stdin);

  ios_base::sync_with_stdio(false);
  cin.tie(0);
  cout.tie(0);

  int T; cin >> T;
  while(T--){
    ll N; cin >> N;
    cout << N * (ll)999999999 << '\n';
  }
  return 0;
}
